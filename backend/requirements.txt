# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0   # PostgreSQL driver (binary wheel, no compilation needed)

# BIS document ingestion
pdfplumber>=0.11.0       # PDF text extraction, page by page

# Semantic / vector retrieval (Milestone 2)
pgvector>=0.3.0                          # SQLAlchemy <-> pgvector column type
sentence-transformers>=3.0.0,<6.0.0      # Local embedding model (no external API/cost)

# LLM-backed answer generation (Milestone 4) — only used if ANSWER_PROVIDER=llm
# and OPENAI_API_KEY is set; imported lazily, so it's not required at runtime
# when the default "extractive" provider is used.
openai>=1.0.0

# Configuration
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
httpx>=0.27.0            # Required by FastAPI's TestClient
